<?xml version="1.0" encoding="UTF-8"?>
<project name="L2JMine" default="copy_updater" basedir=".">
	<description>
        This script will zip the updater folder to shared folder defined into build_configuration.properties   
        
    </description>

	<property file="build_configuration.properties"/>

	<target name="copy_updater"
	  description="Create the output directories. Do nothing if dirs were already created">
		
		<zip destfile="${shared_dist_destination_folder}/L2FrozenRebornUpdater.zip"
		             basedir="${updater_folder}" />

	</target>


</project>